# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.uv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# Node / Vite
node_modules/
.vite/
dist/
.next/
.turbo/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Env / secrets
.env
.env.local
.env.*.local

# Large data artefacts (>50MB per CLAUDE.md). Cleaned outputs are committed.
data/raw/pdfs/
data/raw/*.pdf
data/raw/html/
model/artefacts/*.pkl
model/artefacts/*.joblib

# Vercel
.vercel

# Working-copy scratch images at the repo root. The canonical archived
# source image lives under docs/sources/.
/prediction_report_v4*.jpg
/prediction_report_v4*.png
/1d57a276-*.jfif

# Web build output (regenerated by pnpm build)
web/dist/
*.tsbuildinfo

# Claude Code local-only (machine-specific permission grants)
.claude/settings.local.json

# Web public/data is regenerated by gb_pipeline.export_json; keep the JSON
# but not as a permanent committed artefact (it tracks data/clean).
# Actually we DO want to commit it so Vercel can deploy without running
# the pipeline. Leave it tracked.

# Scratch
scratch/
notebooks/.ipynb_checkpoints/
